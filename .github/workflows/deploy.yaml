name: Deploy

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: Build Project
        run: |
          npm install
          npm run build
      - name: Deploy to Linode
        uses: appleboy/scp-action@master
        with:
          host: '139.162.11.222' #${{ secrets.SSH_HOST }}
          username: 'root' #${{ secrets.SSH_USERNAME }}
          password: 'BpOwOdTk01!' #${{ secrets.SSH_PASSWORD }}
          port: 22 #${{ secrets.SSH_PORT }}
          source: './projects/client/build,./projects/client/package.json,./projects/server/src,./projects/server/package.json,./deploy.config.js,./package*.json'
          target: '${{ secrets.SSH_FOLDER }}'
      - name: Run App
        uses: appleboy/ssh-action@master
        with:
          host: '139.162.11.222' #${{ secrets.SSH_HOST }}
          username: 'root' #${{ secrets.SSH_USERNAME }}
          password: 'BpOwOdTk01!' #${{ secrets.SSH_PASSWORD }}
          port: 22 #${{ secrets.SSH_PORT }}
          script: |
            cd ${{ secrets.SSH_FOLDER }}
            npm install
            pm2 start deploy.config.js
